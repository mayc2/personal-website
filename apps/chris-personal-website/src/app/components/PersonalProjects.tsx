import { Heading } from "../common/styles/Heading"
import { Section } from "../common/styles/Section"

export const PersonalProjects = () => {
    return (
      <Section className='personal' direction="row">
          <Heading primary={true}>Personal Projects</Heading>
      </Section>
    )
}